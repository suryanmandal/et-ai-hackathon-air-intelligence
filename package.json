{
  "name": "vayusense-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && .venv/bin/python3 -m uvicorn src.main:app --reload --host 127.0.0.1 --port 8000",
    "dev": "npm run dev:frontend & npm run dev:backend"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "jspdf": "^4.2.1"
  }
}
